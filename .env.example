# Exemplo de arquivo de variáveis de ambiente
# Copie este arquivo para .env e configure as variáveis conforme necessário

NODE_ENV=production
PORT=3000

# Se estiver usando PostgreSQL (opcional)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/checklist_virtual

# Configurações de sessão (opcional)
# SESSION_SECRET=sua-chave-secreta-aqui

# Configurações de upload (opcional)
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=./uploads